from django.apps import AppConfig

class NotificationsActivityConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'vitalnest.notifications.activities'
    label = 'notifications_activities'